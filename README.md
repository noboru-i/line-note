# line-note

Note app with LINE.

## Running Locally

```sh
$ npm install
$ npm start
```

You can see app by accessing [localhost:5000](http://localhost:5000/).

## Deploying to Heroku

```
$ git push heroku master
$ heroku open
```
